#include <stdio.h>
int main()
{ int n;
    scanf("%d",&n);
    int arr[n];
    for (int i=0; i<n; i++)
    {
        scanf("%d",&arr[i]);
        arr[i]=  arr[i]%10;
    }
  

if(arr[n-1]%10==0)
{
    printf("Yes");
}
else
{
    printf("No");
}

}
